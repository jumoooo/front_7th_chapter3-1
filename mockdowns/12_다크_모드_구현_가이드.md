# 🌙 다크 모드 구현 가이드

> 📅 **작업 일자**: 2025년  
> 📦 **작업 대상**: `packages/after`  
> 🎯 **목적**: 사용자가 직접 다크 모드를 토글할 수 있는 기능 추가

---

## ✅ 구현 완료 사항

### 1. TailwindCSS 다크 모드 설정

**파일**: `packages/after/tailwind.config.js`

```javascript
export default {
  darkMode: ["class"], // class 기반 다크 모드 활성화
  // ...
};
```

- ✅ `darkMode: ["class"]` 설정으로 HTML의 `class="dark"` 속성으로 다크 모드 제어

---

### 2. CSS Variables 다크 모드 지원

**파일**: `packages/after/src/styles/globals.css`

- ✅ `.dark` 클래스 기반 다크 모드 토큰 정의
- ✅ `@media (prefers-color-scheme: dark)` fallback 유지
- ✅ 모든 Semantic Tokens에 다크 모드 값 정의

---

### 3. useTheme Hook 생성

**파일**: `packages/after/src/hooks/useTheme.ts`

**기능**:

- ✅ 테마 상태 관리 (`light` | `dark` | `system`)
- ✅ localStorage에 테마 저장
- ✅ 시스템 테마 변경 감지
- ✅ HTML 요소에 `dark` 클래스 자동 추가/제거
- ✅ `toggleTheme()` 함수 제공

**사용 예시**:

```typescript
const { theme, resolvedTheme, toggleTheme } = useTheme();
```

---

### 4. ThemeToggle 컴포넌트 생성

**파일**: `packages/after/src/components/ui/theme-toggle.tsx`

**기능**:

- ✅ 다크 모드/라이트 모드 토글 버튼
- ✅ 아이콘 애니메이션 (태양/달 아이콘 전환)
- ✅ 접근성 지원 (aria-label)
- ✅ shadcn/ui Button 컴포넌트 사용

---

### 5. ManagementPageHeader에 토글 버튼 추가

**파일**: `packages/after/src/components/domain/management/ManagementPageHeader.tsx`

- ✅ 헤더 우측 상단에 ThemeToggle 버튼 추가
- ✅ 레이아웃 조정 (flex로 정렬)

---

### 6. 초기 테마 설정

**파일**: `packages/after/src/main.tsx`

- ✅ 앱 시작 시 localStorage에서 테마 불러오기
- ✅ 시스템 설정 감지 (fallback)
- ✅ HTML에 `dark` 클래스 자동 적용

---

## 🎯 사용 방법

### 기본 사용

1. **자동 적용**: 앱 시작 시 저장된 테마 또는 시스템 설정이 자동으로 적용됩니다.

2. **토글 버튼**: ManagementPageHeader 우측 상단의 토글 버튼을 클릭하여 다크 모드를 전환할 수 있습니다.

3. **상태 저장**: 선택한 테마는 localStorage에 저장되어 다음 방문 시에도 유지됩니다.

---

### 프로그래밍 방식 사용

```typescript
import { useTheme } from "@/hooks/useTheme";

function MyComponent() {
  const { theme, resolvedTheme, toggleTheme, setTheme } = useTheme();

  return (
    <div>
      <p>현재 테마: {resolvedTheme}</p>
      <button onClick={toggleTheme}>토글</button>
      <button onClick={() => setTheme("light")}>라이트</button>
      <button onClick={() => setTheme("dark")}>다크</button>
      <button onClick={() => setTheme("system")}>시스템</button>
    </div>
  );
}
```

---

## 🔧 작동 원리

1. **HTML 클래스 제어**: `useTheme` hook이 HTML의 `document.documentElement`에 `dark` 클래스를 추가/제거합니다.

2. **CSS Variables**: `.dark` 클래스가 있을 때 다크 모드 CSS Variables가 적용됩니다.

3. **TailwindCSS**: `dark:` prefix를 사용하여 다크 모드 스타일을 적용할 수 있습니다.

   ```tsx
   <div className="bg-background dark:bg-neutral-900">내용</div>
   ```

4. **자동 전환**: CSS Variables를 사용하므로 대부분의 컴포넌트는 자동으로 다크 모드를 지원합니다.

---

## 📝 추가 개선 가능 사항

1. **다른 위치에 토글 버튼 추가**

   - Header 컴포넌트
   - 사이드바
   - 설정 페이지

2. **테마 선택 드롭다운**

   - 라이트/다크/시스템 선택 옵션

3. **애니메이션 개선**

   - 테마 전환 시 부드러운 전환 효과

4. **Storybook에서 테마 테스트**
   - Storybook에도 다크 모드 지원 추가

---

## ✅ 완료 체크리스트

- [x] TailwindCSS `darkMode: ["class"]` 설정
- [x] CSS Variables 다크 모드 토큰 정의
- [x] `useTheme` hook 생성
- [x] `ThemeToggle` 컴포넌트 생성
- [x] ManagementPageHeader에 토글 버튼 추가
- [x] 초기 테마 설정 로직
- [x] localStorage 테마 저장
- [x] 시스템 테마 감지

---

> ✅ **결론**: 다크 모드 토글 기능이 완전히 구현되었습니다!
