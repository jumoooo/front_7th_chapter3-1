# Chapter3-1 프로젝트 셋업 완료 문서

## 📋 목차

1. [셋업 개요](#셋업-개요)
2. [설치된 패키지 및 버전](#설치된-패키지-및-버전)
3. [설정 파일 상세](#설정-파일-상세)
4. [폴더 구조](#폴더-구조)
5. [주요 변경 사항](#주요-변경-사항)
6. [다음 단계](#다음-단계)

---

## 셋업 개요

### ✅ 완료된 작업

After 패키지에 현대적인 디자인 시스템 구축을 위한 모든 필수 도구와 설정이 완료되었습니다.

### 🎯 셋업 목표

- **TailwindCSS 4.x** 기반 디자인 시스템 구축
- **shadcn/ui** 컴포넌트 라이브러리 통합
- **CVA (Class Variance Authority)**를 활용한 variants 패턴
- **React Hook Form + Zod** 폼 관리 시스템
- **React 19** 완전 호환

---

## 설치된 패키지 및 버전

### ✅ 버전 호환성 검증 완료

모든 패키지는 **React 19.2.0**과 호환되도록 검증된 버전으로 설치되었습니다.

### Dependencies

#### 핵심 라이브러리

```json
{
  "react": "^19.1.1",
  "react-dom": "^19.1.1",
  "class-variance-authority": "^0.7.1", // CVA - variants 패턴
  "clsx": "^2.1.1", // 클래스 유틸리티
  "tailwind-merge": "^3.4.0", // TailwindCSS 클래스 병합
  "react-hook-form": "^7.66.1", // 폼 관리 (React 19 호환)
  "@hookform/resolvers": "^5.2.2", // React Hook Form resolvers
  "zod": "^4.1.13", // 스키마 검증
  "lucide-react": "^0.554.0" // 아이콘 라이브러리
}
```

#### Radix UI 패키지 (shadcn/ui가 자동 설치)

```json
{
  "@radix-ui/react-slot": "^1.2.4", // Slot 컴포넌트
  "@radix-ui/react-label": "^2.1.8", // Label 컴포넌트
  "@radix-ui/react-checkbox": "^1.3.3", // Checkbox 컴포넌트
  "@radix-ui/react-select": "^2.2.6" // Select 컴포넌트
}
```

### DevDependencies

```json
{
  "@tailwindcss/vite": "^4.1.14", // TailwindCSS 4.x Vite 플러그인 (권장)
  "@tailwindcss/postcss": "^4.1.14", // PostCSS 플러그인 (선택사항)
  "tailwindcss": "^4.1.17" // TailwindCSS 4.x 코어
}
```

**사용 중인 패키지:**

- `@tailwindcss/vite`: Vite 플러그인 방식 (현재 사용 중)
- `tailwindcss`: TailwindCSS 4.x 코어 패키지

### 📌 버전 호환성 확인 내역

- ✅ **Radix UI**: React 19 지원 확인 (`peer: ^16.8 || ^17.0 || ^18.0 || ^19.0`)
- ✅ **React Hook Form**: React 19 지원 확인 (`peer: ^16.8.0 || ^17 || ^18 || ^19`)
- ✅ **lucide-react**: React 19 지원 확인 (`peer: ^16.5.1 || ^17.0.0 || ^18.0.0 || ^19.0.0`)
- ✅ **CVA, clsx, tailwind-merge**: React와 무관 (호환)

---

## 설정 파일 상세

### 1. Vite 설정 (`vite.config.ts`)

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  // ... 기타 설정
});
```

**변경 사항:**

- `@tailwindcss/vite` 플러그인 추가
- Vite가 직접 TailwindCSS 4.x를 처리하도록 설정
- PostCSS 설정 불필요 (Vite 플러그인이 처리)

### 1-1. PostCSS 설정 (`postcss.config.js`)

```javascript
export default {
  plugins: {
    // TailwindCSS는 Vite 플러그인(@tailwindcss/vite)으로 처리되므로 PostCSS 설정 불필요
    autoprefixer: {},
  },
};
```

**변경 사항:**

- `@tailwindcss/postcss` 제거 (Vite 플러그인이 처리)
- `autoprefixer`만 유지

### 2. TailwindCSS 설정 (`src/styles/globals.css`)

```css
@import "tailwindcss";

:root {
  /* 색상 팔레트 - 기본 slate 테마 */
  --color-background: 0 0% 100%;
  --color-foreground: 222.2 84% 4.9%;
  /* ... 기타 CSS 변수들 */
}
```

**주요 특징:**

- TailwindCSS 4.x의 `@import "tailwindcss"` 방식 사용
- `:root`를 통한 CSS 변수 정의 (Vite 플러그인과 호환)
- shadcn/ui 호환 색상 팔레트 설정
- 다크 모드 지원 준비 (prefers-color-scheme)
- **Vite 플러그인(`@tailwindcss/vite`)이 `@import "tailwindcss"`를 처리**

### 3. Vite 설정 상세

**설치된 패키지:**

- `@tailwindcss/vite`: TailwindCSS 4.x Vite 플러그인
- `tailwindcss`: TailwindCSS 4.x 코어 패키지

**장점:**

- PostCSS 설정 불필요
- Vite와 직접 통합되어 더 빠른 빌드
- `@import "tailwindcss"` 자동 처리

### 4. shadcn/ui 설정 (`components.json`)

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/styles/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

**설정 내용:**

- TypeScript 경로 alias (`@/*` → `./src/*`)
- TailwindCSS CSS 파일 경로 지정
- CSS 변수 기반 테마 사용
- 기본 색상: slate

### 5. 유틸리티 함수 (`src/lib/utils.ts`)

```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

/**
 * 클래스 이름을 병합하는 유틸리티 함수
 * clsx와 tailwind-merge를 결합하여 TailwindCSS 클래스 충돌을 자동으로 해결
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**용도:**

- 컴포넌트에서 조건부 클래스명 병합
- TailwindCSS 클래스 충돌 자동 해결
- shadcn/ui 컴포넌트에서 필수 사용

### 6. 메인 진입점 수정 (`src/main.tsx`)

```typescript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { App } from "./App";
import "./styles/globals.css"; // TailwindCSS 스타일 추가
import "./styles/components.css"; // 기존 컴포넌트 스타일 유지
```

**변경 사항:**

- `globals.css` import 추가 (TailwindCSS 스타일)
- 기존 `components.css`는 유지 (레거시 스타일)

---

## 폴더 구조

### 생성된 디렉토리 및 파일

```
packages/after/
├── components.json                    # shadcn/ui 설정 파일
├── postcss.config.js                  # PostCSS 설정 (TailwindCSS 플러그인 추가)
├── package.json                       # 의존성 추가 완료
└── src/
    ├── lib/
    │   └── utils.ts                   # cn() 유틸리티 함수
    ├── components/
    │   └── ui/                        # shadcn/ui 컴포넌트 디렉토리
    │       ├── button.tsx
    │       ├── input.tsx
    │       ├── label.tsx
    │       ├── checkbox.tsx
    │       ├── textarea.tsx
    │       ├── select.tsx
    │       ├── card.tsx
    │       ├── table.tsx
    │       └── form.tsx
    └── styles/
        ├── globals.css                # TailwindCSS 4.x 설정
        └── components.css             # 기존 레거시 스타일
```

### 설치된 shadcn/ui 컴포넌트

- ✅ **button** - CVA를 활용한 variants 패턴
- ✅ **input** - 폼 입력 필드
- ✅ **label** - 폼 레이블
- ✅ **checkbox** - 체크박스
- ✅ **textarea** - 텍스트 영역
- ✅ **select** - 선택 드롭다운
- ✅ **card** - 카드 컨테이너
- ✅ **table** - 테이블
- ✅ **form** - React Hook Form 통합

---

## 주요 변경 사항

### 1. package.json

**추가된 dependencies:**

- React 19 호환 버전의 모든 필수 패키지
- Radix UI 패키지들 (shadcn/ui가 자동 설치)

**추가된 devDependencies:**

- `@tailwindcss/postcss`: TailwindCSS 4.x PostCSS 플러그인

### 2. PostCSS 설정

- TailwindCSS 4.x 플러그인 추가
- 기존 autoprefixer 유지

### 3. 스타일 시스템

- TailwindCSS 4.x CSS 기반 설정
- shadcn/ui 호환 CSS 변수 정의
- 다크 모드 지원 준비

### 4. 컴포넌트 시스템

- shadcn/ui 컴포넌트 9개 설치 완료
- CVA를 활용한 variants 패턴 적용
- TypeScript 경로 alias 설정

---

## 다음 단계

### 🎯 즉시 시작 가능한 작업

1. **Before 패키지 분석**

   ```bash
   cd packages/before
   pnpm dev
   ```

   - 레거시 코드의 문제점 파악
   - 스타일링 방식 분석
   - 컴포넌트 설계 문제점 정리

2. **After 패키지 개발 시작**

   ```bash
   cd packages/after
   pnpm dev
   ```

   - shadcn/ui 컴포넌트 활용
   - CVA variants 패턴 적용
   - React Hook Form + Zod로 폼 구현

3. **컴포넌트 마이그레이션**
   - Before의 컴포넌트를 After로 점진적 마이그레이션
   - UI와 비즈니스 로직 분리
   - 일관된 컴포넌트 API 설계

### 📚 참고할 문서

- [TailwindCSS 4.x 문서](https://tailwindcss.com/docs)
- [shadcn/ui 문서](https://ui.shadcn.com/)
- [CVA 문서](https://cva.style/docs)
- [React Hook Form 문서](https://react-hook-form.com/)

---

## 체크리스트

### ✅ 완료된 항목

- [x] React 19 호환 버전 검증
- [x] 필수 의존성 설치
- [x] TailwindCSS 4.x 설정
- [x] PostCSS 설정
- [x] shadcn/ui 초기화
- [x] 필수 컴포넌트 설치 (9개)
- [x] 유틸리티 함수 생성
- [x] TypeScript 경로 alias 설정
- [x] CSS 변수 기반 테마 설정

### 🔄 다음 작업

- [ ] Before 패키지 분석 및 문서화
- [ ] After 패키지 컴포넌트 구현
- [ ] ManagementPage 마이그레이션
- [ ] Storybook 설정 (선택사항)
- [ ] Dark Mode 구현 (심화 과제)

---

## 문제 해결 가이드

### 개발 서버 실행

```bash
# 루트에서
pnpm dev:after

# 또는 after 패키지에서
cd packages/after
pnpm dev
```

### 컴포넌트 추가

```bash
cd packages/after
npx shadcn@latest add [component-name]
```

### 의존성 업데이트

```bash
# 루트에서
pnpm install
```

### 빌드 확인

```bash
cd packages/after
pnpm build
```

---

## 버전 정보

- **Node.js**: >=18.0.0
- **pnpm**: v10.23.0
- **React**: 19.2.0
- **TypeScript**: 5.9.3
- **TailwindCSS**: 4.1.14
- **Vite**: 7.1.9

---

## ⚠️ 알려진 이슈 및 해결 방법

### TailwindCSS 4.x `@import "tailwindcss"` 에러

**에러 메시지:**

```
ENOENT: no such file or directory, open 'E:\...\packages\after\tailwindcss'
```

**원인:**

- TailwindCSS 4.x의 `@import "tailwindcss"` 구문이 PostCSS 플러그인에 의해 처리되지 않아 파일로 해석됨
- `@tailwindcss/postcss` 플러그인이 제대로 작동하지 않을 수 있음

**해결 방법 (최종):**

✅ **Vite 플러그인 방식 사용 (권장)**

TailwindCSS 4.x는 Vite 플러그인 방식이 더 안정적입니다.

1. **필수 패키지 설치**

   ```bash
   cd packages/after
   pnpm add -D @tailwindcss/vite tailwindcss@latest
   ```

2. **Vite 설정에 플러그인 추가**

   ```typescript
   // vite.config.ts
   import tailwindcss from "@tailwindcss/vite";

   export default defineConfig({
     plugins: [react(), tailwindcss()],
   });
   ```

3. **PostCSS 설정 수정**

   ```javascript
   // postcss.config.js
   export default {
     plugins: {
       // TailwindCSS는 Vite 플러그인으로 처리되므로 제거
       autoprefixer: {},
     },
   };
   ```

4. **CSS 파일 유지**

   ```css
   /* globals.css */
   @import "tailwindcss";

   :root {
     /* CSS 변수 정의 */
   }
   ```

5. **개발 서버 재시작**

   ```bash
   pnpm dev
   ```

**이전 시도 (참고용):**

- ❌ PostCSS 플러그인 방식: `@tailwindcss/postcss` 사용 시 `@import "tailwindcss"`가 파일로 해석되는 문제 발생
- ✅ Vite 플러그인 방식: `@tailwindcss/vite` 사용 시 정상 작동

**참고:**

- TailwindCSS 4.x는 Vite 프로젝트에서 `@tailwindcss/vite` 플러그인 사용을 권장
- PostCSS 방식보다 Vite 플러그인 방식이 더 빠르고 안정적

---

**셋업 완료일**: 2025년 1월  
**셋업 상태**: ✅ 모든 필수 설정 완료  
**다음 단계**: Before 패키지 분석 및 After 패키지 구현 시작
