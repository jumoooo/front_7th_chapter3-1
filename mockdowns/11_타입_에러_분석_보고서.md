# 🔍 TypeScript 타입 에러 분석 보고서

> 📅 **분석 일자**: 2025년  
> 📦 **분석 대상**: `packages/after/src`  
> 🎯 **목적**: `unknown` 타입 관련 TypeScript 에러 분석 및 해결 방안

---

## 📊 발견된 에러 요약

**총 9개의 타입 에러**가 `packages/after/src/components/organisms/Table.tsx` 파일에서 발견되었습니다.

---

## 🔴 에러 상세 분석

### 파일: `packages/after/src/components/organisms/Table.tsx`

#### 에러 1: Line 129 - Badge userRole prop

```typescript
// ❌ 에러
return <Badge userRole={value} showIcon />;
// Type 'unknown' is not assignable to type '"user" | "admin" | "moderator" | "guest" | undefined'
```

**원인:**

- `value`는 `Record<string, unknown>[columnKey]`로부터 추출되어 `unknown` 타입
- `Badge.userRole`은 `'admin' | 'moderator' | 'user' | 'guest' | undefined` 타입을 요구

**해결 방안:**

- 타입 가드를 사용하여 `value`가 유효한 `userRole` 값인지 확인 후 타입 단언

---

#### 에러 2: Line 153 - row.id를 number로 사용

```typescript
// ❌ 에러
onClick={() => onDelete?.(row.id)}
// Argument of type 'unknown' is not assignable to parameter of type 'number'
```

**원인:**

- `row.id`는 `Record<string, unknown>`에서 추출되어 `unknown` 타입
- `onDelete`는 `(id: number) => void` 타입을 요구

**해결 방안:**

- `row.id`가 `number`인지 확인하는 타입 가드 사용

---

#### 에러 3: Line 173 - ReactNode 타입

```typescript
// ❌ 에러
<Badge type={type} pill>
  {value}
</Badge>
// Type 'unknown' is not assignable to type 'ReactNode'
```

**원인:**

- `value`는 `unknown` 타입
- `Badge`의 `children` prop은 `React.ReactNode` 타입을 요구

**해결 방안:**

- `value`를 `ReactNode`로 변환하거나 타입 단언

---

#### 에러 4: Line 178 - Badge status prop

```typescript
// ❌ 에러
return <Badge status={value} showIcon />;
// Type 'unknown' is not assignable to type '"draft" | "published" | "archived" | "rejected" | "pending" | undefined'
```

**원인:**

- `value`는 `unknown` 타입
- `Badge.status`는 특정 유니온 타입을 요구

**해결 방안:**

- 타입 가드를 사용하여 유효한 status 값인지 확인

---

#### 에러 5-9: row.id를 number로 사용 (여러 위치)

- Line 193: `onPublish?.(row.id)`
- Line 201: `onArchive?.(row.id)`
- Line 209: `onRestore?.(row.id)`
- Line 216: `onDelete?.(row.id)`
- Line 283: `row[column.key]` (ReactNode 타입)

**원인:**

- 모두 `row.id` 또는 `row[columnKey]`가 `unknown` 타입이기 때문

---

## 💡 해결 방안

### 1. 타입 가드 함수 생성

```typescript
// 타입 가드 함수들
const isUserRole = (
  value: unknown
): value is "admin" | "moderator" | "user" | "guest" => {
  return (
    typeof value === "string" &&
    ["admin", "moderator", "user", "guest"].includes(value)
  );
};

const isPostStatus = (
  value: unknown
): value is "draft" | "published" | "archived" => {
  return (
    typeof value === "string" &&
    ["draft", "published", "archived"].includes(value)
  );
};

const isNumber = (value: unknown): value is number => {
  return typeof value === "number";
};

const isReactNode = (value: unknown): value is React.ReactNode => {
  return value !== null && value !== undefined;
};
```

### 2. 각 에러 위치에 타입 가드 적용

---

## 📝 수정 계획

1. **타입 가드 함수 추가** (파일 상단)
2. **Line 129 수정**: `userRole` 타입 가드 적용
3. **Line 153, 193, 201, 209, 216 수정**: `row.id` 타입 가드 적용
4. **Line 173, 283 수정**: `ReactNode` 타입 가드 적용
5. **Line 178 수정**: `status` 타입 가드 적용

---

## ✅ 해결 완료

모든 타입 에러가 해결되었습니다!

### 해결 방법

1. **타입 가드 함수 추가**: `isUserRole`, `isPostStatus`, `isNumber`, `isString` 함수 생성
2. **각 에러 위치에 타입 가드 적용**: 모든 `unknown` 타입을 안전하게 타입 가드로 검증 후 사용
3. **타입 안전성 향상**: `Record<string, unknown>`을 사용하면서도 타입 안전성 확보

### 수정된 파일

- `packages/after/src/components/organisms/Table.tsx`
  - 타입 가드 함수 추가 (Line 123-147)
  - 모든 에러 위치에 타입 가드 적용
  - `unknown` 타입을 안전하게 처리

### 결과

- ✅ 모든 타입 에러 해결
- ✅ 타입 안전성 향상
- ✅ `any` 타입 제거 완료

---

> ✅ **결론**: 모든 타입 에러가 해결되었고, 타입 안전성이 향상되었습니다!
